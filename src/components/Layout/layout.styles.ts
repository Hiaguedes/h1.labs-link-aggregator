import styled from 'styled-components'

const Container = styled.div`
    width:100vw;
    height:100vh;
    display:inline-flex;
    justify-content: center;
    align-items: center;
`;

const MainContent = styled.main`
    padding: 2rem 0;
`;

export const LayoutStyles = { Container, MainContent } 